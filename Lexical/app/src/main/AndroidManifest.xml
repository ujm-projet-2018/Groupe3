<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gentile.lexical">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ConnectionProf" />
        <activity android:name=".ConnectionEleve" />
        <activity android:name=".InscriptionProf" />
        <activity android:name=".MainProf" />
        <activity android:name=".AjoutEleve" />
        <activity android:name=".StatEleve" />
        <activity android:name=".AjoutMot" />
        <activity android:name=".exo1" />
        <activity android:name=".gameOverExo1"></activity>
    </application>

</manifest>